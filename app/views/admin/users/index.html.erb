<h1>Rotten Mangoes</h1>

<hr>
<%= paginate @users %>
<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.full_name %></td>
    <td><%= user.email %></td>
  </tr>
  <% end %>
</table>